 if (find(nums2.begin(),nums2.end(),val)==nums2.end()){ // val is the element to be search in nums2 vector
                temp.insert(val);
            }
temp.clear() // will clear the vector and if temp is set then it will also be cleared